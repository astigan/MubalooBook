package com.example.mubaloobook;

import android.test.InstrumentationTestCase;

/**
 * A test that should always fail, helpful for verifying that the project configuration is setup
 * correctly and can run unit tests
 */
public class SanityTest extends InstrumentationTestCase {

    public void testSanity() {
        assertEquals(true, false);
    }

}
